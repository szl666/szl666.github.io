{
  "build": {
    "env": {
      "RUBY_VERSION": "2.7.6"
    }
  },
  "buildCommand": "gem install bundler:2.2.19 && bundle install && bundle exec jekyll build",
  "outputDirectory": "_site",
  "installCommand": "gem install bundler:2.2.19"
}
